/* 
The most common way to create an array in JavaScript would be to assign that array to a constiable like this.
*/

const superHeroName = ["Superman", "SuperGirl", "Batman", "BatGirl", "Flash"];
console.log(superHeroName);

const friendsAge = [12, 23, 54, 5, 21, 90];
console.log(friendsAge);
